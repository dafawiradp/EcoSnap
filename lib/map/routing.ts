
import L from 'leaflet';

export function calculateRoute(start: L.LatLngExpression, end: L.LatLngExpression): L.LatLngExpression[] {
  // Placeholder for routing logic or integration with routing API
  // For now, return straight line points
  return [start, end];
}
